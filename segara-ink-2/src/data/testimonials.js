// Real client quotes go here once the owner shares them — research only
// turned up an aggregate "100% recommend, 23 reviews" figure on the
// studio's Facebook page, not individual review text, so nothing is
// fabricated here. Add entries as { name, meta, text } and Testimonials.jsx
// will start rendering the scrolling review columns automatically.
export const TESTIMONIALS = []
